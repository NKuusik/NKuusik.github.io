// Pointless balls
/* var canvas = document.getElementById('canvas');
var width = canvas.width;
var height = canvas.height;

for (var i = 0; i < 10; i++)  {
    for (var b = 0; b < 10; b++)    {
        var circlePoint = new Point(20, 20)
        circlePoint.x = circlePoint.x+((width/10)*i);
        circlePoint.y = circlePoint.y+((height/10)*b);
        
        var newCircle = new Path.Circle(circlePoint, 10);
        var circleColor = new Color(Math.random(), Math.random(), Math.random(), Math.random());
        newCircle.fillColor = circleColor;
    }  
} */